import React from "react";
import styled from "styled-components";

const Header = () => {
  return <AppHead>Header</AppHead>;
};

export default Header;

const AppHead = styled.header`
  width: 100%;
  height: 100vh;
`;
